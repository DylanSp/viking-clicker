import * as React from "react";
import { FunctionComponent } from "react";

export const DistanceDisplay: FunctionComponent = () => {
    return (
        <>
            Distance to Target: 90%
        </>
    );
};
